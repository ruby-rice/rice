std::complex
-----------
Similarly to ``std::string``, Rice considers std::complex a Builtin type. That means that ``std::complex`` values are converted to Ruby ``Complex`` instances, and vice versa.
